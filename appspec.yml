version: 0.0
os: linux
files:
  - source: /
    destination: /tmp
hooks:
  BeforeInstall:
    - location: scripts/stop_previous_container.sh
      timeout: 200
      runas: root
  AfterInstall:
    - location: scripts/start_container.sh
      timeout: 200
      runas: root

